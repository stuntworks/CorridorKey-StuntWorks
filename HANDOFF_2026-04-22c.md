# CorridorKey — Code Handoff 2026-04-22 (Session 3)

## WHERE WE ARE
BRAW range processing still produces 0 PNGs. Thread completes in ~1-2 seconds (too fast for AI processing — frames being skipped). Root cause not yet confirmed.

---

## CONFIRMED FACTS THIS SESSION

1. **Thread IS running to completion** — finally block runs, BRAW temp dir gets deleted
2. **Thread completes in ~1-2 seconds** — NOT processing any frames (6 AI frames = 30-60s minimum)
3. **All frames are being skipped** via `if frame is None: continue`
4. **Defender blocks file writes from background threads** — even on K:\, even with try/except. Pattern: first write succeeds (creates file), Defender scans it, all subsequent writes fail silently. This killed all our diagnostic probes.

## ROOT CAUSE CANDIDATES (unconfirmed)
- **Candidate A** — `braw_tif_files` is empty (glob finds 0 TIFFs) → all frames skip
- **Candidate B** — tifffile raises exception for every frame → caught, frame=None, all skip
- **Candidate C** — `_has_tifffile = False` AND `_has_pil = False` → falls through to cv2.imread which returns None for LZW TIFFs

---

## WHAT WAS FIXED THIS SESSION (accumulated from sessions 1-3)

1. Module-level imports: traceback, shutil moved out of thread
2. tqdm silenced: TQDM_DISABLE env var + partialmethod monkeypatch
3. tifffile wired in as primary reader
4. Dtype handling: handles uint16 (>>8), float32/64 (clip+multiply), uint8, other
5. `import torch` removed from finally block → sys.modules.get("torch") instead
6. `on_poll_timer` handles "probe" kind (writes to ck_thread.txt from main thread)

---

## WHAT TO DO NEXT SESSION

### Step 1 — Read the debug log after reloading + running Process Range
The installed file now has a **pre-thread TIFF check** that runs on the main thread (no Defender risk). Look for this in `C:\Users\ragsn\AppData\Local\Temp\corridorkey_debug.txt`:
```
Pre-thread TIFF check: N files in CK_BrawRange_...
  First TIFF: CK_BrawRange_..._00086400.tif, size=XXXXXXX
```

If `N = 0` → Candidate A confirmed. The glob is finding nothing.
If `N = 6` → Candidates B or C. Look for per-frame probe file on K:\ (see Step 2).

### Step 2 — Check the unique probe file on K:\
`_probe_path = f"K:\\ck_p{int(time.time())}_{os.getpid()}.txt"`
After running Process Range, check K:\ for a file named `ck_p*.txt` with today's timestamp.
If it has all probes through PROBE 5 and per-frame entries → tells us exactly what's happening.
If it has only PROBE 1 → Defender still locking (unlikely with unique filename).

### Step 3 — Check CorridorKey Log widget in Resolve UI
The thread puts messages to `_ui_queue` as "log" → they appear in the Log widget.
After running, the widget should show either:
- `BRAW frames: 6 TIFF files` (files found)  
- `BRAW frames: 0 TIFF files` (empty — Candidate A)
- `Read error at frame 86400: <exception message>` (Candidate B)

---

## FILES
- Installed: `C:\ProgramData\Blackmagic Design\DaVinci Resolve\Fusion\Scripts\Utility\CorridorKey.py`
- Source: `D:\New AI Projects\CorridorKey-Plugin\resolve_plugin\CorridorKey_Pro.py`
- Debug log: `C:\Users\ragsn\AppData\Local\Temp\corridorkey_debug.txt`
- Thread probe: `K:\ck_p*.txt` (unique per run, new in this session)
- Output dir: `K:\C_key test\CK_A001_02091940_C017\`

## KEY INSTALLED CODE LOCATIONS
- Pre-thread TIFF check: ~line 1664 (main thread, writes to debug log)
- `_probe_path` unique file: ~line 1679 (inside _run())
- tifffile read + dtype handling: ~line 1766-1795 (inside _run() try block)
- `on_poll_timer` probe handler: ~line 1894 (writes probe to ck_thread.txt)
- finally block: ~line 1854

## EXPECTED RESULT AFTER FIX
6 PNGs: `CK_A001_02091940_C017_000000.png` through `_000005.png`
Processing takes 30-120 seconds (not 1-2 seconds).

---

## NOTES
- VozType CUDA conflict explained this session: two independent CUDA contexts (PyTorch vs CTranslate2). VozType will detect via pynvml and auto-yield GPU. CorridorKey side: add `_warmup = torch.zeros(1).cuda(); del _warmup` near module load (after CorridorKeyProcessor import) so CUDA inits once at DaVinci startup, not on first button click.
- Consensus AI (9/9): rated 7/10 for this session — good suggestions (>>8, cvtColor, ascontiguousarray) but missed the Defender file-locking root cause.
