# CorridorKey — Handoff 2026-04-23i

## FILES
- Plugin: `C:\ProgramData\Blackmagic Design\DaVinci Resolve\Fusion\Scripts\Utility\CorridorKey.py`
- Viewer: `C:\ProgramData\Blackmagic Design\DaVinci Resolve\Fusion\Scripts\Utility\preview_viewer_v2.py`

---

## BLANK SCREEN — NOT A CODE BUG

Log shows:
```
SAM2 output gate applied — alpha mean 0.363 -> 0.000
Matte after norm — min:0 max:255 mean:0.0
```

The `sam2_mask.png` in SESSION_DIR is stale from a previous session — it marks a region of the frame where the actor NO LONGER IS. The neural alpha (0.363 mean) gets multiplied by the mask (5.5% coverage, wrong location) → result is essentially zero everywhere → blank composite.

**Fix for the user:** In the live viewer, hit CLEAR → confirm → re-do SAM2 clicks on the current frame → APPLY MASK.

**Do NOT change any code** — the gate logic is correct. The mask is just pointing at the wrong spot.

---

## AUDIO POPS — PARTIALLY FIXED, ROOT CAUSE UNCLEAR

### What we've done:
- `CREATE_NEW_CONSOLE` → `DETACHED_PROCESS` on braw-decode.exe calls (no conhost.exe spawned)
- PollTimer now adaptive: 500ms during processing, 5000ms when idle (10x fewer Resolve main-thread interrupts)
- Dead render-queue code with `StartRendering` removed from `_export_braw_range_to_frames`
- Path 0 (direct `ExportCurrentFrameAsStill`, no timeline switch) for PROCESS FRAME — confirmed working in log

### What's still happening:
- Pop when Resolve starts = Resolve's own audio engine init. Plugin can't control this.
- Pops "the whole time" while plugin is open — PollTimer fix should help but user hasn't confirmed improvement yet
- User uses Focusrite for ALL audio work — changing Resolve's audio device is NOT acceptable

### If pops continue after PollTimer fix:
- Investigate whether the Fusion scripting event loop itself (not just our timer) causes audio interrupts
- Consider: can we stop the PollTimer entirely when idle and restart it only when a button is clicked?
- The viewer subprocess is confirmed clean (no audio-affecting ops found by agent audit)

---

## BRAW DECODE EXE — STILL FAILING (but PATH fix not yet tested)

Last log showed `rc=3221225477` (0xC0000005) on the `-n` info query. At the time of that test, the PATH fix had NOT yet been applied.

**Fixes now in the code (not yet tested for BRAW decode):**
1. Resolve install dir added to subprocess `PATH` env var — should let BlackmagicRawAPI.dll find its dependencies
2. `DETACHED_PROCESS` replaces `CREATE_NEW_CONSOLE` — no audio pop from exe launch
3. `time` import bug fixed (`_time2.time()` in `_try_braw_decode_exe`)
4. `subprocess` scope bug fixed in `on_close`

**Next test:** Click PROCESS RANGE on a BRAW clip. Look for:
- `BRAW decode exe: <W>x<H>` = success
- `info failed (rc=...)` = still failing — need to check if BlackmagicRawAPI.dll is actually loading

---

## WHAT'S WORKING (confirmed in log)

- `alpha_method` flip-flop FIXED — sam2_mask.png existence now forces `alpha_method=1`
- Path 0 WORKING: `OK (Path 0 — direct still, no timeline switch)` — no SetCurrentTimeline call, no audio pop on PROCESS FRAME
- JumpSlider applied to all 3 sliders — click locks on, 1:1 tracking
- Throttle render pattern — immediate on first touch, 60ms cooldown
- Despeckle init — 300ms delayed render, no more cycle-to-fix
- CLEAR button now shows confirm dialog before deleting SAM2 mask
- Resolve close fix — `taskkill /F /T` kills viewer + all child processes (process tree)

---

## ALL FIXES APPLIED THIS SESSION (summary)

| Fix | File | Status |
|-----|------|--------|
| BRAW decode Popen: `CREATE_NEW_CONSOLE` → `DETACHED_PROCESS` | CorridorKey.py | Applied |
| BRAW decode: `time` not imported → `_time2.time()` | CorridorKey.py | Applied |
| BRAW decode: Resolve dir added to subprocess PATH | CorridorKey.py | Applied |
| `on_close` `subprocess` scope bug: `subprocess.CREATE_NO_WINDOW` → `_sp.CREATE_NO_WINDOW` | CorridorKey.py | Applied |
| `_merge_live_params`: sam2_mask.png existence forces `alpha_method=1` | CorridorKey.py | Applied |
| Path 0: direct ExportCurrentFrameAsStill (no timeline switch) for PROCESS FRAME | CorridorKey.py | Applied |
| `on_close`: `taskkill /F /T` kills entire viewer process tree | CorridorKey.py | Applied |
| PollTimer: adaptive 500ms active / 5000ms idle | CorridorKey.py | Applied |
| Dead render-queue code (StartRendering) removed from `_export_braw_range_to_frames` | CorridorKey.py | Applied |
| JumpSlider: all 3 sliders lock-on to click, 1:1 mouse tracking | preview_viewer_v2.py | Applied |
| Render throttle: immediate first render + 60ms cooldown (was debounce) | preview_viewer_v2.py | Applied |
| Despeckle init: 300ms delayed render, racing singleShot(0) removed | preview_viewer_v2.py | Applied |
| Despill + despeckle sliders: `_render_now()` → `_schedule_render()` | preview_viewer_v2.py | Applied |
| CLEAR button: confirm dialog before deleting sam2_mask.png | preview_viewer_v2.py | Applied |
| `on_update`: merged params committed to `self._params` BEFORE render | preview_viewer_v2.py | Applied |

---

## START NEXT SESSION WITH

```
CorridorKey Pro — continue from 2026-04-23i handoff.
Load: D:\New AI Projects\CorridorKey-Plugin\HANDOFF_2026-04-23i.md

Priority:
1. Confirm audio pops are gone or reduced (PollTimer adaptive fix)
2. Test BRAW decode exe — PROCESS RANGE on BRAW clip, check log for "info failed" vs resolution
3. User needs to CLEAR stale SAM2 mask and re-apply — blank screen is NOT a code bug
```
