# CorridorKey — Handoff 2026-04-23c

## ROOT CAUSE CONFIRMED
Windows Defender hooks BOTH IRP_MJ_CREATE (file opens) AND IRP_MJ_WRITE (file writes)
from untrusted background threads. The numpy pre-decode fixed file opens. But the
thread still hangs on ANY file write — probes, PNG saves, everything.

## EVIDENCE
- Probe file stops at PROBE 6 every run (not a crash — a HANG)
- Queue-based probe routing added but poll timer timing made it hard to verify
- No PNG output in K:\C_key test\CK_A001_02091940_C017\ for Apr 23 runs
- Resolve hang on close = background thread stuck in blocked write

## CURRENT CODE STATE
Installed file: C:\ProgramData\Blackmagic Design\DaVinci Resolve\Fusion\Scripts\Utility\CorridorKey.py
Version tag: [v0423-queue] (probe uses _ui_queue now)
Granular probes between PROBE 6 and PROBE 7 are in place (6a, 6b, 6c)
PROC-A through PROC-F probes around chroma/cvtColor/process_frame are in place

## WHAT TO DO FIRST
Add Defender exclusions via Windows Security GUI (Tamper Protection blocks PowerShell):
1. Start → Windows Security → Virus & threat protection → Manage settings
2. Scroll to Exclusions → Add or remove exclusions
3. Add folder: C:\Users\ragsn\AppData\Local\Temp\corridorkey_renders
4. Add folder: K:\C_key test
5. ALSO add: C:\Users\ragsn\AppData\Local\Temp (for probe files)

After adding exclusions:
- Run Process Range in Resolve
- Read ck_thread.txt: powershell -Command "Get-Content 'C:\Users\ragsn\AppData\Local\Temp\ck_thread.txt'"
- Also check output folder for new PNGs

## IF EXCLUSIONS WORK
The thread should run through all 6 frames and write PNGs to K:\C_key test\.
Then clean up: remove all PROBE/PROC probes per the cleanup list in HANDOFF_2026-04-23b.md

## IF EXCLUSIONS DON'T WORK — ARCHITECTURE FIX
Route ALL file output through the main thread:
- _probe: already uses _ui_queue ("probe" kind) → ck_thread.txt 
- PNG saves: put computed fg/matte arrays into a new _save_queue
- Main thread on_poll_timer processes _save_queue and calls cv2.imwrite()
- This means NO file I/O from background thread whatsoever

## KEY FILES
- Installed: C:\ProgramData\Blackmagic Design\DaVinci Resolve\Fusion\Scripts\Utility\CorridorKey.py
- Source: D:\New AI Projects\CorridorKey-Plugin\resolve_plugin\CorridorKey_Pro.py (NOT synced, installed file is authoritative)
- Output: K:\C_key test\CK_A001_02091940_C017\
- Probe (queue): C:\Users\ragsn\AppData\Local\Temp\ck_thread.txt
- Probe (direct): C:\Users\ragsn\AppData\Local\Temp\ck_p{ts}_{pid}.txt

## NUMPY DECODE — CONFIRMED WORKING
Pre-decode on main thread is correct and working. "Decoded 6 frames to numpy" appears in log.
Warmup also runs chroma_hint_gen + cvtColor on main thread before thread starts.
The frame data pipeline is solid. The only remaining issue is FILE OUTPUT from the thread.

## AICONSENSUS RESULT (2026-04-23 01:34)
9 AIs reviewed the architecture. Unanimous: main-thread decode + closure pass is correct.
Threading (not multiprocessing) is right for embedded Python.
Queue/buffer pool is production hardening, not required for 6-frame batch use case.
File saved: D:\New AI Projects\Claude-to-Kimi\responses\ (check session history)
