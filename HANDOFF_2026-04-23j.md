# CorridorKey — Handoff 2026-04-23j

## FILES
- Plugin: `C:\ProgramData\Blackmagic Design\DaVinci Resolve\Fusion\Scripts\Utility\CorridorKey.py`
- Viewer: `C:\ProgramData\Blackmagic Design\DaVinci Resolve\Fusion\Scripts\Utility\preview_viewer_v2.py`

---

## AUDIO ISSUE — ONGOING, ROOT CAUSE UNCLEAR

### What we know:
- Audio degradation started during the BRAW session yesterday
- Happens even with Resolve fully closed
- Unplugging/replugging Focusrite fixes it temporarily but it comes back
- Power plan switched to **Ultimate Performance** (fixed USB suspend issue)
- USB selective suspend disabled
- Windows audio services restarted (net stop/start AudioSrv)
- Two browsers (Brave + Chrome) both running YouTube — user says this was always fine before

### Suspects not yet eliminated:
- **NVIDIA GeForce Overlay** — hooks audio for game recording, known to conflict with Focusrite ASIO. User was asked to exit it from system tray and test.
- **braw-decode.exe crash aftermath** — BlackmagicRawAPI.dll crashes during DLL_PROCESS_ATTACH (rc=3221225477). May have registered a partial COM audio component that keeps re-initializing.

### What NOT the cause:
- Plugin code (plugin is only running inside Resolve — can't affect audio when Resolve is closed)
- The BRAW fallback (was causing 145 timeline switches which hammered audio engine, but that's fixed now)

---

## FIXES APPLIED THIS SESSION

| Fix | File | Status |
|-----|------|--------|
| CLEAR dialog hidden behind viewer (WindowStaysOnTopHint) | preview_viewer_v2.py | Applied |
| CLEAR not restoring actress — NN alpha backup (alpha_nn_backup.png) | preview_viewer_v2.py | Applied |
| CLEAR must call reload_pngs() + _render_now() not _repaint_both() | preview_viewer_v2.py | Applied |
| _save_live_params_now writes alpha_method:0 when points empty | preview_viewer_v2.py | Applied (by agent) |
| BRAW fallback: SetCurrentTimecode seek replaces 145 temp timeline creations | CorridorKey.py | Applied |
| BRAW fallback: on-demand TIFF decode — was loading ~11GB all at once | CorridorKey.py | Applied |
| BRAW decode: DETACHED_PROCESS → CREATE_NO_WINDOW + stdin=DEVNULL | CorridorKey.py | Applied |
| BRAW decode: PATH diagnostic log before subprocess call | CorridorKey.py | Applied |
| Path 0: gate fixed (ok0 falsy check removed — trust file existence) | CorridorKey.py | Applied |
| _live_watcher: slows to 500ms after 2s idle (was 50ms always) | preview_viewer_v2.py | Applied |
| singleShot(0) → singleShot(16) in render finally block | preview_viewer_v2.py | Applied |
| PollTimer adaptive interval in finally: block | CorridorKey.py | Applied |
| Power plan: Balanced → Ultimate Performance | Windows | Applied |
| USB selective suspend: disabled | Windows | Applied |

---

## BRAW DECODE EXE — STILL FAILING

```
BRAW decode exe: info failed (rc=3221225477) stderr=''
```

- `BlackmagicRawAPI.dll` confirmed at `C:\Program Files\Blackmagic Design\DaVinci Resolve\`
- PATH injection confirmed in log: `BRAW_SDK_PATH='C:\Program Files\Blackmagic Design\DaVinci Resolve'`
- Crash happens during DLL_PROCESS_ATTACH — before main() runs — empty stderr
- The fallback now works (SetCurrentTimecode per frame, no timeline switch)
- Fallback is slow (~1-3s per frame) but audio-safe

**Root cause hypothesis:** BlackmagicRawAPI.dll may require being inside the Resolve process to initialize. Running as a child subprocess from Resolve's embedded Python may not provide the required process context.

---

## PROCESS RANGE — NOW WORKING (via fallback)

Log confirmed: 145 frames exported and keyed successfully via fallback.
BUT: no output sequence appeared — the AI processing ran but we don't know if save_output() wrote files.

**Next test:** Run PROCESS RANGE again with the new code. Check K:\C_key test\CK_A001_02092020_C039 for output PNGs.

---

## REMAINING ISSUES (from old code audit)

| # | Issue | Status |
|---|-------|--------|
| 1 | Despeckle loads wrong on first open | FIXED (singleShot 300ms) |
| 2 | Slider drag locks up on 4K | FIXED (_schedule_render) |
| 3 | SAM2 mask disappears between clicks | NOT FIXED — add log to _clear_sam_points |
| 4 | Despeckle starts disabled every session | NOT CHECKED |
| 5 | No progress feedback during PROCESS RANGE | PARTIAL — status() updates exist but main thread blocks UI refresh |

---

## START NEXT SESSION WITH

```
CorridorKey Pro — continue from 2026-04-23j handoff.
Load: D:\New AI Projects\CorridorKey-Plugin\HANDOFF_2026-04-23j.md

Priority:
1. Confirm audio issue resolved (NVIDIA Overlay was suspect — did exiting it fix it?)
2. Test PROCESS RANGE — did output PNGs appear in K:\C_key test\?
3. Fix remaining issues #3, #4, #5
4. Fix braw-decode.exe if possible (may require running inside Resolve process)
```
