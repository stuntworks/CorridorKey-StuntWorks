# CorridorKey — Handoff 2026-04-23l

## FILES
- Plugin: `C:\ProgramData\Blackmagic Design\DaVinci Resolve\Fusion\Scripts\Utility\CorridorKey.py`
- Viewer: `C:\ProgramData\Blackmagic Design\DaVinci Resolve\Fusion\Scripts\Utility\preview_viewer_v2.py`
- GitHub: `github.com/stuntworks/CorridorKey-Plugin` — commit `1bc8636`

---

## AUDIO STATUS
- Focusrite Notifier pops on Resolve start — this is normal, Resolve claims ASIO on startup
- Both Resolve and Focusrite now set to 256 buffer
- Audio was stable during this session — no degradation observed

---

## SAM2 FIXES APPLIED THIS SESSION (not yet tested end-to-end)

### Fix 1 — Panel: Static gate fallback (committed `1bc8636`)
When `sam2_video_masks` is empty (points lost on restart) but `sam2_mask.png` exists on disk,
the range loop now loads the static gate lazily on the first frame and applies it to all frames.

### Fix 2 — Viewer: sam_positive/sam_negative persisted to live_params.json
Engine copy already had this. Installed copy was stale. Synced.

### Fix 3 — Viewer: SAM2 points restored on startup
`_restore_sam_points_from_disk()` added to `__init__`. Reads `sam_positive`/`sam_negative`
from `live_params.json` and restores dots to canvas on viewer open.

### Fix 4 — Viewer: Startup write block (ROOT CAUSE FIX)
`_on_despeckle_toggled` was firing via `singleShot(0)` on startup and overwriting
`live_params.json` with empty SAM2 points before restoration could finish.
Fix: `_startup_saves_blocked = True` for 100ms on startup. Unblocked after event loop settles.

---

## NOT YET TESTED
- SINGLE FRAME → viewer open → SINGLE FRAME again → does sam2_mask.png still exist?
- 145-frame Process Range WITH SAM2 mask applying correctly to output

---

## STILL BROKEN
- CLEAR button — deletes sam2_mask.png file but does NOT clear dots visually, does NOT
  update live_params.json, does NOT re-render. Looks like nothing happened.

---

## START NEXT SESSION WITH

```
CorridorKey Pro — continue from 2026-04-23l handoff.
Load: D:\New AI Projects\CorridorKey-Plugin\HANDOFF_2026-04-23l.md

Priority:
1. Test SAM2 end-to-end: SINGLE FRAME → viewer open → SINGLE FRAME again → sam2_mask.png still EXISTS?
2. Test Process Range with SAM2 — do output frames have mask applied?
3. Fix CLEAR button — needs to: clear dots visually, update live_params.json, delete sam2_mask.png, re-render
4. Push viewer fixes to git (viewer is in engine folder, not tracked)
```
