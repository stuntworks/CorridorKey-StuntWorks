# Part 08. Gotchas and Warnings

**Scope: every solved trap that already bit once, with its fix. Scan this first when a bug smells familiar. Grouped by area for scanning; each row still carries Gotcha / What happens / Root cause / Fix / Evidence / Last verified.**

---

## Process Range / batch hangs (Resolve)

"PROCESS RANGE" (batch/range keying) has been the single biggest time sink on this project. The entries below are distinct bugs found across different weeks in the same feature, not one bug with several names.

| Gotcha | What happens | Root cause | Fix | Evidence | Last verified |
|---|---|---|---|---|---|
| PROCESS RANGE: background thread silent death (early) | Batch keying in a background thread dies with no error, no log, no traceback | Resolve's embedded Python sets `sys.stdout = None` for daemon threads; the first `print()` call crashes the thread silently before any frame is processed | Run PROCESS RANGE synchronously on the main thread (UI freezes during the batch, accepted tradeoff). Never re-add background threading here without first solving `sys.stdout=None` | archive/corridorkey_session_20260420.md | 2026-04-20 |
| PROCESS RANGE: Fusion UIManager PollTimer dies during BRAW render-wait | Range processing hangs specifically on BRAW source clips; UI never recovers, Stop/Start cannot revive it | Fusion's UIManager `PollTimer` permanently dies once an event handler blocks 30-60 seconds (the BRAW render-wait loop); `QApplication.processEvents()` is a different event system and cannot substitute | Route BRAW post-render work synchronously on the main thread (frames are already decoded in memory by that point) | BRAW_RANGE_DEBUG_REPORT_2026-04-23.md | 2026-04-23 |
| BRAW export range off-by-one | The last frame of a BRAW range export is silently dropped | `ss + (out_f - cs)` computed the export end as exclusive when Resolve's timeline end is exclusive already, dropping one real frame | `ss + (out_f - cs) + 1` | BRAW_RANGE_DEBUG_REPORT_2026-04-23.md | 2026-04-23 |
| PROCESS RANGE: 2-month silent hang, the big one | Range jobs on 4K HEVC source hang with no error after roughly 210 frames; six-plus theories chased over two months (thread-safety fixes, CUDA-init timing, cv2.VideoCapture backend swap, POS_FRAMES seek theory, worker-thread-kill "Path B") all failed to reproduce or fix it | `container.close()` inside a PyAV `finally` block deadlocks libav's internal cleanup specifically on 4K HEVC streams after a few hundred frames | Skip `container.close()` entirely; it is a temporary object the garbage collector reclaims anyway, so nothing is leaked by not calling it explicitly | session-handoff-2026-05-23-ck-range-hang-solved-stage2-pending.md | 2026-05-23 |
| PROCESS RANGE: stale-backup regression during the above saga | A fresh session investigating the hang restored an older "known-good" backup that was actually older than that morning's real working baseline, undoing several already-fixed bugs | No manifest existed describing which fixes lived in which backup; a session formed new theories without first reading existing handoffs for the same multi-session bug | Read existing memory/handoffs for a bug already spanning multiple sessions before forming new theories. deploy.py's manifest.json (added later) is the structural fix for the missing-manifest half of this | KNOWLEDGE_LOG_ARCHIVE:2657-2678 | 2026-05-13 |

---

## SAM2, merge, and matte-quality traps

| Gotcha | What happens | Root cause | Fix | Evidence | Last verified |
|---|---|---|---|---|---|
| HSV alpha-hint trap | Tan, khaki, and olive fabric gets flagged as screen color; NN produces gray holes inside the body on affected wardrobe | `AlphaHintGenerator`'s HSV hue range (35 to 85) overlaps warm fabric tones at low saturation, and its 5x5 morphological close/open collapses hair into a binary blob the NN cannot recover | Inline RGB chroma test (green dominates R and B by roughly 20 percent, float32, single Gaussian blur, no morphology), kept in `generate_alpha_hint`; `AlphaHintGenerator` (HSV) is banned via an explicit DANGER ZONE comment and never called | KNOWLEDGE_LOG_ARCHIVE:1899-1997; technique-corridorkey-alpha-hint-hsv-trap.md; resolve_plugin/CorridorKey_Pro.py:1410-1425 | 2026-04-27, regressed and re-fixed 2026-04-27 |
| Black-pants matting holes are not automatable | Attempts to auto-fill NN dropout holes on black pants via a color discriminator all fail | Black pants and hair both carry the same dark green spill signature; no reliable color-only rule separates a real hole from hair | Dropped as a color-discriminator problem. Treated as a manual-mask job (SAM2 dots), not something to solve algorithmically | KNOWLEDGE_LOG_ARCHIVE:3822-3831 | 2026-06-15 |
| Chest/butt SAM holes | Chest and butt regions drop out of the matte partway through a batch range | PNG-square video-predictor bug; full detail and graveyard entry in Part 04 | Feed the video predictor native, unpadded frame shape (JPEG q=95) at all three call sites | reference-ck-sam2-png-square-breaks-video-propagation.md | 2026-06-29 |
| Chroma-weight Gaussian ghost bands | Any Gaussian blur on the SAM/chroma weight produces a visible 50 percent ghost band at soft edges | Stacking a second softness source (Gaussian) on top of an already-soft weight value double-softens the transition; a CK value of 0.5 blended with a SAM value of 1.0 shows as a visible 0.75 halo | Never blur the weight or chroma_score; use a hard smoothstep-only soft band instead. Standing rule, Part 01 Rule 4 | corridorkey_sam_merge.py:481-483 | 2026-05-01 |
| SAM sanity valve amputates clean CK | A "SAM clips CK only when covering 50%+ of CK's dominant figure" safety valve clips good CK output on easy, well-lit clips | The valve's coverage threshold does not distinguish "SAM is correctly clipping a real error" from "SAM is simply smaller than CK on an easy clip" | Reverted same day; no coverage-threshold valve of this shape has shipped since | git commits 336c61e through 77e30f1 | 2026-05-08 (approx, from ledger dating) |
| Narrow-feather merge chosen by a fooled metric | A narrow-feather merge variant wins a Sobel-gradient edge-quality gauntlet and ships as default, then looks worse than the prior day to Berto's eye | The Sobel metric scored hair wisps as "better" that were actually artifacts; the gauntlet's dots were auto-generated (NN centroid), not Berto's real click pattern, invalidating the comparison entirely | Reverted. Law: artist eye beats an automated metric; no gauntlet verdict counts without Berto's real dot pass (Part 01 Rule 3) | session-handoff-2026-06-11-ck-zone-era-recipe-receipts.md; session-handoff-2026-06-12-ck-fusion-v2-trimap-architecture-born.md | 2026-06-12 |
| Fusion V2 toggle invisible, twice | Berto judges "Fusion V2 results" for a full session while the toggle for it was never actually clicked; recurred a second time in a different UI location | The Fusion V2 engine toggle sat at the bottom of a settings drawer with zero visual prominence and zero log confirmation when off; an invisible feature reads as a nonexistent one | Moved to a visible twin toggle on the main toolbar. Law: an invisible feature is a nonexistent feature | session-handoff-2026-06-12-ck-fusion-v2-trimap-architecture-born.md (recurred 2026-07-05) | 2026-07-05 |
| ck_authority wire resurrection | Enabling `ck_authority` (CK overrides SAM wherever green evidence exists) brings back support wires that SAM had correctly removed | `ck_authority` cannot distinguish a wire hugging the body from a real harness/body edge; measured to resurrect 521 of 625 (83%) wire pixels on the wire-regression ground-truth clip | Only ever safe on the unified_band engine after its post-band shape/dot-kill re-check; the flag is deliberately never wired to the operator panel on the garbage_matte engine | corridorkey_sam_merge.py:1686-1712; ae_plugin/cep_panel/index.html:2175 | 2026-07-06 |
| mask2_bypass UI/disk desync | The MASK 2 bypass checkbox visibly toggles in the UI but silently does nothing to the actual render | The checkbox's UI state and its on-disk persisted value went out of sync at commit 6c047ec0 and stayed that way for a whole evening of SUBTRACT/carpet-kill tuning that was actually testing the wrong state | Fixed via an 8-agent convergent diagnosis that found and closed the sync bug | session-handoff-2026-05-18-overnight-ck-chroma-cc-and-body-core.md | 2026-05-18 |
| Legacy CEP folder shadows the live junction | Berto unknowingly tests an old, stale engine build for an unknown span of time, producing confusing "nothing I fix seems to work" verdicts | A leftover pre-junction CEP folder (`com.corridorkey.panel.OLD-pre-junction-2026-04-26`) sat on disk and shadowed the live junction | Found and removed; this explains an earlier "toggle never worked" confusion that had been blamed on other causes | session-handoff-2026-06-14-ck-clean-engine-pivot-and-preview-grain.md | 2026-06-14 |
| Strap Bridge slider was dead for months | A slider is tuned and re-tuned for months with no measurable effect on output | Only wired into the `experimental_recipe` branch, which the panel never actually sends; output at slider value 4 and slider value 40 was md5-identical | Renamed to Junk Kill pending removal, later pulled entirely | project-ck-ux-punchlist-2026-06-16.md | 2026-06-16 |
| Zone tool wiped by an init race | The draggable-ellipse zone tool looks completely dead for a full night of debugging | Drawing the zone before the first auto-key completed let a later init step silently overwrite it; a pure timing race, not a logic bug | Early-init 200ms restore, plus permanent instrumentation logging so the race is visible if it recurs | session-handoff-2026-06-11-ck-zone-era-recipe-receipts.md | 2026-06-11 |
| Feet-zone guard over-fires near frame bottom | SAM matte tighten and feet/calf erosion controls appear dead on a 6K side-profile clip; first suspected as a UI or cache bug | `_body_exits_bottom` guard, built for waist-crop shots, over-fires on any full-body shot whose feet sit near the frame bottom, silently disabling the feet zone entirely | Resolution-invariant (fraction-of-height) gap/taper detection; the guard now only fires on genuine crops | reference-ck-feet-zone-guard-near-bottom.md | 2026-07-16 |
| DOT LAW: vest/whip holes were two conflated problems | Vest and whip matte holes get treated as one regression to root-cause for months with no fix that sticks | Two legitimately different hole classes were conflated: `fill_body_holes` had silently been saving these renders through most of June until a 07-03 verdict disabled it, and the camera genuinely sees real green through motion blur in some frames | Auto-enable enclosed-hole fill whenever SAM dots are present, except where the source pixel is actually screen-colored (the "DOT LAW"); live-confirmed AE/Premiere parity | reference-ck-dot-law-fill.md; session-handoff-2026-07-19-ck-premiere-dotlaw-ships.md | 2026-07-19 |
| Fusion Comp sidecar writes silently all failed | Fusion Comp mode produces zero sidecar mattes for an unknown prior span, with no visible error | `_write_fusion_sidecars` imported `compute_region_matte` from `corridorkey_sam_merge`, a function that was never implemented; Python's all-or-nothing import killed the entire import statement, so every sidecar write failed silently every frame | Split the import; guard the write with an explicit `if compute_region_matte is not None` check | session-handoff-2026-05-26-ck-fusion-importerror-ui-research.md | 2026-05-26 |
| Range vs single-frame merge dispatch drift | Batch range renders show less CK body detail than the single-frame preview of the identical frame | Range export called `apply_sam2_gate_subtract` directly on the union SAM mask; single-frame preview used `_panel_dispatch_sam2_combine` (per-mask). Two different call paths silently diverged | Route range processing through the same per-mask dispatch single-frame preview uses | REVIEW_2026-05-14_save-queue-fix.md #3; resolve_plugin/CorridorKey_Pro.py:2619-2649, 3641-3646, 3986-3991 | 2026-05-14 |
| SAM bbox exploded by one stray pixel | The keyed subject vanishes on a frame with a single reflection or dust speck near the body | `_get_sam_bbox` computed the SAM bounding box from the raw mask including stray pixels; one outlier pixel expands the bbox enough that dilation swallows the whole frame | Largest-connected-blob filter plus an outlier guard before computing the bbox | docs/reviews/2026-06-16-corridorkey-ae-multi-angle-review.md CK-3; commit f6b43775 | 2026-06-16 |
| SAM2 override math introduces a purple halo | Filling body holes via `final = where(sam==1, 1, weight*ck)` fixes the holes but adds a visible purple halo at body edges | The override math changes behavior right at the body edge, not just inside the hole interior | Reverted; used `binary_fill_holes(sam)` instead, which only touches enclosed interior regions | session-handoff-2026-05-16-ck-chroma-weight-sam-override.md | 2026-05-16 |
| Wire vs body-crease separation is not solvable by threshold | Grid-searching a distance/chroma threshold from 3 to 60px across a wide range never cleanly separates a support wire from a real body crease | Wire and crease are physically fused in the image; there is no pixel-level signal that distinguishes them in the general case | Partial shape-based classification plus operator negative dots; accepted as a tradeoff (Berto: easier to remove a wire by hand than get a butt back that was wrongly cut) | session-handoff-2026-07-05-ck-ae-unified-edge-engine.md | 2026-07-05 |
| Frame-0 SAM copy regression on moving subjects | On a moving subject, frame 0 silently wears frame 1's silhouette (the wrong pose); shipped invisibly, since 100 prior test clips happened not to hit the case | An unconditional copy of the frame-1 mask onto the frame-0 mask slot (`sam_video_masks` index 0 set from index 1), added to fix a coverage-collapse edge case, applied even when the subject was moving and frame 0's own mask was fine | Make the copy conditional on `start_frame == 0` only, matching DaVinci's original conditional | commit 71d7f610 | 2026-06-25 |
| Feet-clean bake overfit to one demo frame | A feet-cleaning pass (min CK_ONLY bottom-30% against the merged alpha, an off-green "tighten" pass) looked great on the one demo frame it was built against, then wiped the CK matte's bottom on other frames where SAM ran sparse-low | Tuned and judged against a single frame instead of the required multi-clip corpus (Part 01 Rule 3) | Reverted both passes the same session | KNOWLEDGE_LOG_ARCHIVE:3970-3976 | 2026-06-27 |
| SAM2 mask_input bias from an external NN matte | Feeding the CK/NN binary matte into SAM2 as a `mask_input` bias ("Option B") produced, at best, no visible difference and at worst a regression on multi-actor/harness/hair clips | SAM2's `mask_input` is a feature-space bias trained on SAM2's own internal priors; an externally-produced binary matte is out of that distribution | v5 design: the NN matte only ever expands the SAM2 silhouette where the NN independently confirms body, monotonic, never removes area | KNOWLEDGE_LOG_ARCHIVE:2258-2264; decision-corridorkey-option-b-evaluated.md | 2026-05-01 |
| External reviewers missed an existing fix on the client-facing AE path | ChatGPT, GLM, and a third external reviewer each analyzed an AE despill bug in isolation, unaware a subtractive-only fix (commit 239291c8) already existed for the identical symptom elsewhere in the codebase | `CorridorKeyModule/core/color_utils.py:241-242`'s "average" despill mode still did an additive `r += spill*0.5; b += spill*0.5` on the client-facing AE engine path specifically; the existing fix had landed in the viewer copies but never reached this path, a case of drift between duplicated despill implementations distinct from the 2026-04-29 viewer-copy incident above | Ported the subtractive-only removal into the AE engine path as well | bus chat corridorkey 2026-06-24 19:54-20:56 | 2026-06-24 |
| SCRUB RANGE assumed one global SAM anchor frame for every click | If a scrub range excluded the anchor frame, click points landed on the substitute frame's coordinates instead, and SAM2 segmented the green wall behind the actor rather than the actor | A single global `sam_anchor_frame` was used for every stored click, instead of tracking which frame each click was actually made on | Per-click frame tracking (`sam_clicks` stores a frame number with each point); SCRUB RANGE now hard-refuses a click made across a frame boundary with a clear error instead of silently mis-segmenting | HANDOFF_VIDEO_PROPAGATION.md; KNOWLEDGE_LOG_ARCHIVE:1899-1925 | 2026-04-27 |
| Flying-frame wedge: dark junk near the actor's head survives cleanup | On dirty frames, a dark, non-green wedge near the actor's head survives every cleanup pass | Compound cause: the hair-zone rule trusted CK verbatim in the top 35% band, CK only removes green (so a dark non-green wedge passes as foreground), the wedge touches the body so connectivity-based speckle-kill cannot isolate it, and the SAM margin-save counts it as body-connected. An earlier "ck-green" pixel-color test fix had already been rejected because it failed on black pants | Green-geography gate: trust CK in the hair zone only where the pixel sits inside the actual physical green-screen region, judged by screen geography rather than pixel color | bus chat corridorkey 2026-06-13; session-handoff-2026-06-13-ck-overnight-review-fixes-and-wedge.md | 2026-06-13 (first cut, narrower than the final fix) |
| torch.autocast added to the compile warm-up pass broke live-view sync | Adding `torch.autocast` to the `_compile()` warm-up dummy pass, intended to fix jagged first-preview edges, instead desynced the AE live preview from the playhead, and the jagged edges it was meant to fix were still present | Not confirmed that session; the change was fully reverted before a root cause was pinned down | Fully reverted. The jagged-edge complaint was later resolved a different way, by leaving the compile/refiner path out of that code path entirely | KNOWLEDGE_LOG_ARCHIVE:1794-1836 | 2026-04-25 |

---

## Preview / render parity

| Gotcha | What happens | Root cause | Fix | Evidence | Last verified |
|---|---|---|---|---|---|
| Preview and render used two different SAM2 engines | Weeks of patching symptoms (SAM noise, green fringe, feather halo) one at a time overnight, believing preview and render were "the same engine, differently tuned" | Preview (`cmd_sam_apply`) used SAM2's image predictor; render (`cmd_batch`) used the video predictor. Two genuinely different engines will never match; the matte Berto tuned in preview was never what actually rendered | Unified post-processing into one canonical `apply_matte_postproc`; a "Match Render" toggle runs the real video predictor in preview too | session-handoff-2026-06-22-ck-preview-render-unify-and-sam-root.md; session-handoff-2026-06-28-ck-preview-render-parity.md | 2026-06-28 |
| Sampled preview vs full-res render, two-path model | Every bug that session traced back to preview and render simply being different code paths | A downscaled/sampled scrub preview and a separate full-res render path guarantee preview does not equal render, structurally, forever | Berto directive: kill the sampled preview. One render only, full-range and full-res, lands on the timeline; review scrubs the actual rendered frames (Part 01 Rule 5) | decision-ck-single-render-review-no-double-render.md | 2026-06-30 |
| Duplicate despill/merge logic per viewer copy | A subtractive-only despill fix landed in the engine but yellow shirts still looked pink for hours in live viewer testing | Resolve's and AE's viewers each inline a duplicate `_ViewerColorUtils` copy (to dodge an eager torch import); the engine-side fix never reached either viewer copy | Port any despill/merge fix into both viewer copies in lockstep; grep both viewer copies on any `color_utils.py` patch | technique-corridorkey-yellow-shirt-despill.md; feedback-corridorkey-viewer-local-despill-trap.md | 2026-04-29 |
| Second parallel alpha path bypassed new sliders | A newly wired despill/combine control does nothing for two straight weeks despite the source clearly showing it wired | Resolve's viewer has a second, parallel alpha path in `PersistentWindow._render_now()` (the matte-view branch) calling orphaned `_combine_one_mask`/`union_alpha` helpers that bypass every new control | Grepped all call sites and migrated both paths. Rule: when pixels contradict a source read, the bug is in a path not yet touched | KNOWLEDGE_LOG_ARCHIVE:2465-2517 | 2026-05-06 |
| Qt window removal orphaned all tuning controls | Removing the Qt popup preview causes every slider (despill, edge, choke, and about 22 others) to silently revert to hardcoded defaults, in both preview and render | The Qt window was the only writer of `live_params.json`; the panel's `getSettings()` read from that file, so once nothing wrote it, settings fell back to a hardcoded `despill=1.0` maximum | Panel now owns the controls directly; preview and render share one `getSettings()` source of truth, no file handoff | technique-corridorkey-qt-window-removal-orphaned-all-controls-2026-06-01.md | 2026-06-01 |
| AE preview and render ran different post-proc engines | Berto tunes the key on a different engine than what actually ships | AE's live preview still ran the old `apply_matte_postproc` path while the render path had moved to Fusion V2 + zone | Wired Fusion V2 + zone into `cmd_single` (the preview path) as well; verified parity | docs/reviews/2026-06-16-corridorkey-ae-multi-angle-review.md CK-1 | 2026-06-16 |
| Believed-clean Resolve path was a false premise | A session tries to rebuild AE onto what it believes is DaVinci's simpler "simple-combine" render path, on the theory Resolve stays clean because it uses less logic | Three-agent code verification found Resolve's render calls the SAME complex garbage-matte path AE uses; an earlier session had mistaken Resolve's preview-viewer math for its actual render path | Scrapped the rebuild; fixed AE's real, divergent bugs instead of chasing a simpler architecture that did not exist | decision-2026-06-22-ck-shared-console-viewer-ecosystem.md | 2026-06-22 |

---

## Adobe / CEP integration

| Gotcha | What happens | Root cause | Fix | Evidence | Last verified |
|---|---|---|---|---|---|
| A bare "--" in manifest.xml kills the whole extension | The CEP extension never appears in Window > Extensions, with no error UI anywhere | CEP silently rejects an entire extension if a comment inside `manifest.xml` contains `--` (here, from `--enable-nodejs --mixed-context` written inline in an XML comment) | `ET.parse()` the manifest before copying it into place, so a malformed comment is caught before deploy, not after | KNOWLEDGE_LOG_ARCHIVE:1605-1610 | 2026-04-13 |
| CEP caches index.html across reloads | Editing `index.html` and reloading only the panel serves stale HTML for hours | CEF aggressively caches the panel page; a panel-only reload does not invalidate that cache | Fully quit AE, delete `%LOCALAPPDATA%\Temp\cep_cache\`, reopen. Change one visible label per edit as a reload tripwire | reference-corridorkey-cep-panel-reload.md | 2026-06-02 |
| Editing corridorkey_sam_merge.py and expecting the change live | A merge-module edit appears to do nothing after a redeploy | `deploy.py` only copies `CorridorKey_Pro.py`; a stale compiled `.pyc` plus a stale already-running viewer subprocess both keep the old merge code executing | Delete `__pycache__\corridorkey_sam_merge.cpython-*.pyc` and fully restart Resolve/AE after every merge-module edit | session-handoff-2026-05-27-ck-garbage-matte-architecture.md; reference-corridorkey-preview-viewer-not-in-deploy.md | recurring, last verified 2026-05-27 |
| host.jsx shell calls used unquoted concatenated paths | A clip path containing a quote or semicolon can execute arbitrary shell code | `system.callSystem()` concatenated an unescaped path directly into a shell command string | `cep.process.createProcess` with an argv array instead of a concatenated string | CODE_REVIEW_2026-04-14.md C1 | 2026-04-14 |
| eval() used as a JSON parser on CEP settings | Any DOM value containing `);` can inject arbitrary ExtendScript | Settings payloads were parsed with `eval()` instead of a real JSON parser | Switched to a real JSON parser | CODE_REVIEW_2026-04-14.md C2 | 2026-04-14 |
| PlayerDebugMode shipped on in production | Chromium's remote-debug port (7777) stays open indefinitely; combined with `--mixed-context`, this is a localhost RCE surface inside AE/Premiere | The debug flag was a dev convenience left enabled in the production installer path | Made dev-only, stripped from the production install path; `--allow-unsigned` is now an explicit opt-in flag, not a silent default | CODE_REVIEW_2026-04-14.md C3/C4; install.py header | 2026-04-14, install.py default confirmed off 2026-07-22 |
| Model reloaded every frame in AE single-frame mode | A 300-frame single-frame-mode shot triggers 300 cold model loads | No model caching existed across repeated single-frame calls | Cache the model across calls, same pattern the main keyer path already used | CODE_REVIEW_2026-04-14.md C6 | 2026-04-14 |
| corridorkey_path.txt trusted unconditionally | A user-writable config file gets prepended to `sys.path` with no validation, which is arbitrary Python execution inside the host app if that file is ever attacker-controlled | The path from `CORRIDORKEY_ROOT` / `corridorkey_path.txt` was prepended to `sys.path` without checking it actually points at a real CorridorKey install | Validate the path against known repo markers before importing anything from it | REVIEW_2026-05-14_codex_release_blockers.md; ae_plugin/ae_processor.py:47-73 | 2026-05-14 |
| window.resizeTo() crashes the CEP host | The panel process crashes | `window.resizeTo()` is not safe inside `CEPHtmlEngine.exe` | Never call it; resize the panel through CEP's own sizing APIs | project-ck-ux-punchlist / CLAUDE-MAP protected list | 2026-06-16 |

---

## Premiere / After Effects frame math

| Gotcha | What happens | Root cause | Fix | Evidence | Last verified |
|---|---|---|---|---|---|
| playerPos returns the next frame boundary | A single-frame key extracts the frame AFTER the parked playhead | Premiere's `seq.getPlayerPosition().seconds` reports frame N+1 when the playhead sits on frame N | Subtract one sequence-frame of time in `ppro_getFrameInfo` before using it | ALIGNMENT.md Behavior 1; commit 804295a | 2026-04-13 |
| Sequence fps used to index source media | A 47-frame batch drifts progressively; frame 1 is off by a hair, frame 47 by two frames | JSX-side frame math used the sequence's fps, not the source clip's native fps, to index into the source media | Return the source-media window as seconds; Python opens the clip and computes frame indices using the source's own `cv2.CAP_PROP_FPS`, never on the JSX side | ALIGNMENT.md Behavior 2 | 2026-04-14 |
| Imported still defaults to 5 seconds | A single keyed PNG placed on the timeline spans 5 seconds instead of 1 frame | Premiere's default duration for an imported still is roughly 5 seconds; `overwriteClip` uses whatever duration the projectItem currently has | Trim the projectItem's in/out points to exactly one frame before placement | ALIGNMENT.md Behavior 3 | 2026-04-14 |
| Batch placement needed +1 but single-frame did not | Fixing single-frame alignment breaks batch alignment by one frame, or vice versa | The -1 offset that fixes single-frame's next-boundary quirk already compensates it; stacking another +1 nudge on top double-corrects. Batch mode needs its own +1 because the dummy `output_00000.png` consumes one timeline frame that single-frame placement does not have | `ppro_importFrame`: no nudge. `ppro_importSequence`: +1 frame nudge. The two paths are not symmetric and must not be made to match each other | ALIGNMENT.md Behavior 4 | 2026-04-14 |
| Imported PNG sequence ignored V1's frame rate | A 47-frame batch drifts against V1 even though single-frame keying was pixel-perfect | Premiere's numbered-stills importer applies the sequence/project default frame rate to an imported PNG sequence, ignoring the footage's actual intended rate; a 24fps source on a 23.976 sequence drifts | `ppro_getInOutInfo` reads V1's real `FootageInterpretation.frameRate` and `ppro_importSequence` applies that exact rate via `setOverrideFrameRate` (or `setFootageInterpretation`) before placement | ALIGNMENT.md Behavior 5 | 2026-04-14 |
| Premiere drops the first frame of an imported sequence | The user's actual keyed range appears to be missing its first frame | The numbered-stills importer skips the lowest-numbered file in the sequence, by design | `ae_processor.py` writes a dummy `output_00000.png` (a copy of the first real keyed frame) so the real range survives the importer's skip | ALIGNMENT.md "Related Issues" | 2026-04-14 |
| targetBin ignored on numbered-stills import | Imported sequences land in the project root instead of the CorridorKey bin | Premiere's importer ignores `targetBin` specifically for numbered-stills imports | Import to root, diff `root.children` before/after to find the new item, then `moveBin()` it into the CorridorKey bin | ALIGNMENT.md "Related Issues" | 2026-04-14 |
| Mattes in the main output folder confuse the importer | Premiere's importer misidentifies frames when two PNG patterns share a folder | `output_NNNNN.png` and `matte_NNNNN.png` in the same directory make the numbered-stills importer guess wrong | Mattes now live in a separate `outDir/mattes/` subfolder | ALIGNMENT.md "Related Issues" | 2026-04-14 |
| sourceFrame used for keying math directly | Reintroduces the seq/source fps drift (Behavior 2) | `sourceFrame`, computed at sequence fps, was used for the keying math instead of only as the SAM2 click anchor | `sourceFrame` is ONLY the SAM2 click anchor; keying math always uses `sourceTimeSeconds` plus the source's own fps | ae_plugin/cep_panel/jsx/host.jsx:859-864 | 2026-07-22 (verified in current tree) |
| Nest/precomp falls back to sequence dimensions | A rendered layer rescales and pixel alignment breaks | When source dimensions are missing, the nest/precomp sizing fell back to the sequence's dimensions instead of the actual source | PNG header dimensions are the primary source of truth for nest/precomp sizing | ae_plugin/cep_panel/jsx/host.jsx:1113-1118 | 2026-07-22 (verified in current tree; line numbers drifted from the 2026-07-22 citation due to the 2026-07-28 matte-picker feature added above this code) |
| .sqpreset path resolved via $.fileName | The wrong sequence preset can load depending on how CEP loaded the panel | `$.fileName` can resolve against `Premiere.exe` itself when CEP loads via `ScriptPath`, not the panel's own folder | Panel-supplied path is tried first; `$.fileName` is only a fallback | ae_plugin/cep_panel/jsx/host.jsx:1454-1463 | 2026-07-22 (verified in current tree; line numbers drifted from the 2026-07-22 citation due to the 2026-07-28 matte-picker feature added above this code) |

---

## DaVinci Resolve specific

| Gotcha | What happens | Root cause | Fix | Evidence | Last verified |
|---|---|---|---|---|---|
| GetStartFrame() subtracted twice | Every seek clamps to frame 0 on some timelines | `clip.GetStart()` is already absolute; subtracting `timeline.GetStartFrame()` from `cf` a second time sends the relative-vs-absolute math deeply negative | Do not subtract; a guard comment marks this in place | resolve_plugin/CorridorKey_Pro.py:829-833 | broke April 2026, guarded since |
| CAP_PROP_POS_MSEC seeks one frame early | Non-integer-fps clips (23.976, 29.97) seek to the wrong frame | Millisecond-based seeking floors/rounds unevenly on non-integer fps (24fps = 41.666ms per frame); the rounding error lands one frame early | Keep `CAP_PROP_POS_FRAMES` (exact for the all-intra formats this project targets), never switch to `CAP_PROP_POS_MSEC` | resolve_plugin/CorridorKey_Pro.py:834-837 | guarded, still current |
| StartRendering() resets the Windows audio engine | Focusrite/ASIO/WDM audio devices drop out during a BRAW/camera-raw still export | Resolve's render-queue path (Path B) resets the system audio engine as a side effect of `StartRendering()` | Use `ExportCurrentFrameAsStill` (Path A) for BRAW/.cin/.dng/.ari stills on Resolve 18.5+; Path B is banned for this case | resolve_plugin/CorridorKey_Pro.py:2399-2410 | guarded, still current |
| Opening BRAW TIFFs from a background thread stalls silently | A BRAW export step hangs with no error | Windows Defender scans any file opened from a background thread, even a read-only open, and the scan can stall the thread indefinitely | Pre-read the TIFF bytes into a `BytesIO` buffer on the MAIN thread first, then hand the buffer to the background thread | resolve_plugin/CorridorKey_Pro.py:5240-5249 | guarded, still current |
| "Disable source clip" disables the whole track | Keying one of five shots on a shared video track hides all five, not just the keyed one | `timeline.SetTrackEnable("video", track, False)` operates on the entire track; there is no per-clip disable in the Resolve API this plugin found | Default the checkbox to unchecked, as a footgun stopgap; the real per-clip API was not found | KNOWLEDGE_LOG_ARCHIVE:2759-2764 | 2026-05-18 |
| Single-instance lock false-positives | "CorridorKey Already Open" appears even when no other instance is running | A stale heartbeat lock file from a crashed prior session was not being treated as stale quickly enough | 15-second staleness window on the heartbeat lock file before treating a lock as abandoned | resolve_plugin/CLAUDE-MAP/parts/live-panel.md | 2026-07-22 (per CLAUDE-MAP) |

---

## BRAW decode

| Gotcha | What happens | Root cause | Fix | Evidence | Last verified |
|---|---|---|---|---|---|
| cv2.imread cannot read Resolve's BRAW-export TIFFs | Every frame silently returns `None`; the thread exits "cleanly" having written zero PNGs | This OpenCV build has no LZW compression support, and Resolve's BRAW-export TIFFs are RGB16 LZW | `PIL.Image.open().convert("RGB")` reads LZW natively | technique-corridorkey-range-thread-2026-04-22.md | 2026-04-22 |
| Raw BRAW decode too flat to key | Chroma key cannot grab green on decoded BRAW footage | SDK decodes flat log-space color by default; a later fix forcing native color for everything then starved the key from the other direction (full detail in Part 04) | Two-stream decode: key on a Rec.709 color-science override, ship native color for the composite | reference-corridorkey-cannot-read-braw.md | 2026-07-18 |

---

## Deploy, install, and git hygiene

| Gotcha | What happens | Root cause | Fix | Evidence | Last verified |
|---|---|---|---|---|---|
| write_plugin.py deployed a stale plugin copy | A deploy silently nukes live sub-pixel slider edits that were never in the embedded copy | `write_plugin.py` embedded an OLD hardcoded plugin copy that predated the margin/soften sliders | Rewrote to read from the live `resolve_plugin/CorridorKey_Pro.py`; superseded entirely by `deploy.py`, kept only as a marked-deprecated fallback | RECOVERED_SUBPIXEL_PANEL_CODE.md; write_plugin.py:12 | 2026-04-26 |
| Clean install let the dead dummy overwrite the canonical file | A clean install ships keying with no SAM batch support at all | `install.py`'s copy step let the root-level `ae_processor.py` (a 729-line stale dummy) overwrite the canonical 1180+ line `cep_panel/ae_processor.py` | Skip the root dummy in the copy step | install.py:238-246 | 2026-05-29 |
| Two CEP copies edited independently | The whole engine (`CorridorKeyModule/`, `backend/`) is found missing from `origin/main`; real work never actually left the local machine (the "193/97 incident": 193 local commits diverged silently) | The AE-installed path and the repo path were two separate copies, edited independently with no sync step and no branch protection to catch the divergence | Replaced the copy-based install with a Windows junction so the installed path and the repo path are physically the same files; added GitHub branch protection (blocks force-push and delete on origin/main, `enforce_admins=true`) | SESSION_HANDOFF.md; CLAUDE.md | 2026-04-26 |
| Stale local origin/main used for release prep | Release-branch prep is based on a commit that still contains internal docs already scrubbed from the real public repo | A local `origin/main` ref was assumed current without fetching first | Always `git fetch` before basing release work on `origin/main`; verify public contents with `gh api` rather than trusting the local ref | KNOWLEDGE_LOG_ARCHIVE:2789-2792 | 2026-05-18 |
| INSTALL.md documented a nonexistent upstream repo | Following the documented engine-fetch step hard-blocks every cold install | INSTALL.md instructed `git clone cnikiforov/CorridorKey`, a hallucinated GitHub user/repo that does not exist | Documented the engine as bundled in the StuntWorks repo clone itself, with the real upstream attribution (nikopueringer/CorridorKey, corridordigital.com) for credit only | KNOWLEDGE_LOG_ARCHIVE:2759-2764 | 2026-05-18 |
| Bulk-restore from .deploy_backups with no manifest | Restoring a backup to fix one thing silently un-fixes everything that happened after that backup was taken; the same regression (HSV hair-killer, zeroed sliders, reverted scrub keying) was reintroduced three times in one day | No manifest recorded which fixes lived in which backup snapshot | Re-applied fixes manually each time; flagged as the project's highest-cost structural problem. `deploy.py`'s later `manifest.json` per backup is the durable fix | KNOWLEDGE_LOG_ARCHIVE:1937-1996; HANDOFF_2026-04-27_FRESH_START_DECISION.md | 2026-04-27 |
| Bisecting against the wrong "clean" commit | A render-regression bisect is anchored to a commit believed clean based on the wrong assumption | Assumed "the May-8 revert-deploy render is clean"; pixel stats proved that render was byte-identical to the dirty output, because `deploy.py --revert` only swaps the panel script; engine/viewer/merge modules still load from the source repo's HEAD regardless of the panel revert | Re-anchored the bisect to the actual engine commits (April 26-30) instead of the panel-revert commit | session-handoff-2026-05-11-corridorkey-render-regressions.md | 2026-05-11 |
| BRAW range export silently fell back to slow Resolve seek+still path for 12 days | Every render for 12 days took 3-5 minutes instead of 30-60 seconds, with no error surfaced | `_export_braw_range_to_frames`'s fast path called `.GetMediaPoolItem()` on an object that was already a MediaPoolItem (an already-extracted clip was passed in, not a raw clip); the resulting `None` triggered a caught-and-swallowed exception, falling back silently to the slow path | Pass the clip itself, not an already-extracted MediaPoolItem, at the call site | session-handoff-2026-05-11-corridorkey-render-regressions.md | 2026-05-11 |

---

## Process and review discipline

| Gotcha | What happens | Root cause | Fix | Evidence | Last verified |
|---|---|---|---|---|---|
| claude-bridge advisor audit run without a directory grant | $0.75 spent on an audit that concludes "the bridge is a poor fit," which is wrong | The advisor's working directory was the tool folder only, with zero read access outside it, so it could not see the actual code it was asked to audit | Grant `--dir` in advisor mode; generalizes to every cross-folder bridge call | KNOWLEDGE_LOG_ARCHIVE:3068-3070 | 2026-06-01 |
| A bug-finding agent's fix recommendation trusted at face value | A recommended fix (anchor a dot at startFrame) would have mispaired dot coordinates if applied | The review agent correctly found the bug but its prescribed fix was wrong; bug-finding and fix-prescribing are different skills | Treat this class of agent as trustworthy for finding bugs, not for prescribing fixes; a human or a higher-judgment reviewer decides the fix | KNOWLEDGE_LOG_ARCHIVE:3449-3451 | 2026-06-06 |
| NN-alone matte gap between AE and DaVinci was a bad comparison | AE and DaVinci appear to key differently on the same footage | The comparison used a raw `cmd_single` PNG on one host against viewer-despeckled output on the other, on a simplified test frame, not a matched test | Ran the same frame with the same settings on both hosts; the pipelines matched once inputs were actually controlled | KNOWLEDGE_LOG_ARCHIVE:1980-1993 | 2026-04-27 |
| Three same-day reverts with empty commit bodies | Three separate changes with strong local evidence behind them (shadow-kill boundary variant, a 5-fix precision-restore stack for hair, BRAW native-color decode) were each reverted the same day with no reason recorded in the commit | No process existed requiring a revert to state why, even under time pressure | Not yet fixed as a process rule; flagged so a future retry does not have to rediscover why each one failed from scratch (Part 13) | commits 0a09146 to e7f72f8; 8580f8a to 774b58a; 31605c1 to 1ea6f2c | 2026-06 (exact dates not separately logged) |
| Cross-project contamination from NoWire | A CK code comment cites a NoWire test clip as CK proof; a NoWire model-sizing constraint (SAM3.1 too large) gets wrongly imported as a CK requirement | Evidence and theories were borrowed across two related but separate StuntWorks projects without verifying they actually applied to CK | Standing rule: never borrow clips, models, or claims across CK and NoWire (Part 01 Rule 7); any CK comment referencing NoWire is junk to be removed | feedback-keep-corridorkey-and-nowire-separate.md | 2026-06-22 |
