__pycache__/
*.pyc
*.pyo
.venv/
*.trt
*.aex
*.zip
*.dll
*.pdb
*.pyd
_backup_working/
.deploy_backups/
.env
fusion_v2_weights/

# Broker secret — machine-local, auto-generated on first run; never commit
ae_plugin/cep_panel/ck_broker_config.json

# --- heavy binaries: never belong in git (2026-06-28, accidental ComfyUI/SAM sweep) ---
.venv-sam3-merged-DEFERRED/
ClipsForInference/
sam2_weights/
CorridorKeyModule/checkpoints/
*.pth
*.onnx
*.pt
*.mov
*.mp4

# --- scratch and debug material (2026-08-25) ---
# WORK/ is THE place for screenshots, debug renders, one-off test output and
# anything else that is worth keeping on disk but is not part of the product.
# It is never committed. Put scratch here instead of at a drive root or in the
# home folder, and the filing sweep will leave it alone.
WORK/

# --- build bible: private engineering notes, NOT for the public repo (2026-08-30) ---
# The bible is 100% StuntWorks engineering — no upstream Corridor material — and it
# documents the SAM merge reasoning, failed approaches and open issues. It lives in the
# PRIVATE repo stuntworks/build-bible under bibles/corridorkey/. Files stay on disk here;
# git just stops publishing them.
docs/build_bible/
